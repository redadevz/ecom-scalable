craftable-pro:
    models:
        LoyaltyCardType:
            table: loyalty_card_types
            skip_model: false
            columns:
                id: id
                name: string
                description: 'text nullable'
                is_active: boolean
                created_at: 'datetime nullable'
                updated_at: 'datetime nullable'
            listing:
                id: 'sortable searchable'
                name: 'sortable searchable'
                description: 'sortable searchable'
                is_active: 'sortable searchable'
                created_at: sortable
            form:
                columns:
                    -
                        cards:
                            -
                                fields:
                                    - name
                                    - description
                                    - is_active
            translatable: {}
            publishable: none
            media_collections: {}
            relationships: {}
            api:
                index:
                    columns:
                        - id
                        - name
                        - description
                        - is_active
                        - created_at
                        - updated_at
                store:
                    columns:
                        - name
                        - description
                        - is_active
                update:
                    columns:
                        - name
                        - description
                        - is_active
                show:
                    columns:
                        - id
                        - name
                        - description
                        - is_active
                        - created_at
                        - updated_at
                destroy: {}
            features: {}
