craftable-pro:
    models:
        Payment:
            table: payments
            skip_model: false
            columns:
                id: id
                invoice_id: 'id foreign'
                payment_method_id: 'id foreign'
                payment_no: 'string nullable'
                amount: decimal
                cash_paid: 'decimal nullable'
                cash_change: 'decimal nullable'
                payment_time: 'datetime nullable'
                comments: 'string nullable'
                created_at: 'datetime nullable'
                updated_at: 'datetime nullable'
            listing:
                id: 'sortable searchable'
                invoice_id: 'sortable searchable'
                payment_method_id: 'sortable searchable'
                payment_no: 'sortable searchable'
                amount: 'sortable searchable'
                cash_paid: 'sortable searchable'
                cash_change: 'sortable searchable'
                payment_time: sortable
                comments: 'sortable searchable'
                created_at: sortable
            form:
                columns:
                    -
                        cards:
                            -
                                fields:
                                    - invoice_id
                                    - payment_method_id
                                    - payment_no
                                    - amount
                                    - cash_paid
                                    - cash_change
                                    - payment_time
                                    - comments
            translatable: {}
            publishable: none
            media_collections: {}
            relationships: {}
            api:
                index:
                    columns:
                        - id
                        - invoice_id
                        - payment_method_id
                        - payment_no
                        - amount
                        - cash_paid
                        - cash_change
                        - payment_time
                        - comments
                        - created_at
                        - updated_at
                store:
                    columns:
                        - invoice_id
                        - payment_method_id
                        - payment_no
                        - amount
                        - cash_paid
                        - cash_change
                        - payment_time
                        - comments
                update:
                    columns:
                        - invoice_id
                        - payment_method_id
                        - payment_no
                        - amount
                        - cash_paid
                        - cash_change
                        - payment_time
                        - comments
                show:
                    columns:
                        - id
                        - invoice_id
                        - payment_method_id
                        - payment_no
                        - amount
                        - cash_paid
                        - cash_change
                        - payment_time
                        - comments
                        - created_at
                        - updated_at
                destroy: {}
            features: {}
